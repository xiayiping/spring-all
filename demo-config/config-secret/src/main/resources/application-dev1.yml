server:
  port: ${all-configs.server-port.config-secret}
  ssl:
    enabled: false

spring:
  application:
    name: ${all-configs.app-name.config-secret}
  cloud:
    vault:
      enabled: false
    aws:
      region:
        static: ap-east-1
  config:
    import:
#      - vault://
      - aws-secretsmanager:dev/crypto-test

secret:
  ref: "${xyp/a/b/d}"