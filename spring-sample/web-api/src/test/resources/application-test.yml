
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:mem:mydb"
    username: sa
    password: sa
    hikari:
      connection-test-query: "select 1"
  jpa:
    properties:
      hibernate:
        default_schema: test
    database-platform: org.hibernate.dialect.H2Dialect
  security:
    oauth2:
      resourceserver:
        opaquetoken:
          introspection-uri: http://a
          client-id: b
          client-secret: c
