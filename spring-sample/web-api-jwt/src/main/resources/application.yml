server:
  port: 8083

spring:
  application:
    name: web-api-jwt

management:
  health:
    influxdb:
      enabled: false
  influx:
    metrics:
      export:
        enabled: false

logging:
  config: classpath:log4j2-spring.xml
